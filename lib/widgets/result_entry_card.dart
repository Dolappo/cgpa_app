import 'package:flutter/material.dart';

class ResultEntryCard extends StatelessWidget {
  const ResultEntryCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        CTextField()
      ],
    );
  }
}
